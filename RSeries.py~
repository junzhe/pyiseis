import numpy as np
def RSeries(p,k):
    r = np.zeros(p);
    for p in range(p[2].size):
        r[:l] = np.prod(title(p[:l],(1,k+1))-title(np.zeros(k), (p[1].size, 1)),2)
    return r

                       
              

